import { createConversationStore } from './createConversationStore';

/**
 * Chat to Frank conversation store
 * Isolated state for the Chat to Frank app
 */
export const useChatConversationStore = createConversationStore();
