import { createConversationStore } from './createConversationStore';

/**
 * Pitch Agent Script conversation store
 * Isolated state for the Pitch Agent Script app
 */
export const usePitchConversationStore = createConversationStore();
